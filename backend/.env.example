# Путь к папке с data.json (необязательно; по умолчанию — в папке ОС для временных файлов)
# DB_PATH=./data

# Обязательно в production: длинная случайная строка для подписи JWT
# JWT_SECRET=ваш-длинный-секретный-ключ

# Срок действия токена (по умолчанию 30d)
# JWT_EXPIRES_IN=30d

# Порт сервера (по умолчанию 3002)
# PORT=3002

# Хост (по умолчанию 0.0.0.0)
# HOST=0.0.0.0

# --- Опционально: SMTP для отправки счетов на email ---
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@example.com

# --- Реквизиты в счетах (опционально) ---
# COMPANY_NAME=ERP Квадрат
# COMPANY_ADDRESS=
# COMPANY_TAX_ID=

# --- Подсказки адресов (DaData, опционально) ---
# DADATA_API_KEY=
